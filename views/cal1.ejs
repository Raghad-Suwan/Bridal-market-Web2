<!--Dress & flower & cake & beauty salon-->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservation Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styleReservation&Calendar.css">
    <link href="/css/navbar.css" rel="stylesheet">

</head>

<body>
    <% if(typeof errorMessage !=='undefined'){ %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <h6 style="color: red;">
                <%= errorMessage %>
            </h6>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% } %>
       <%-include('../views/partials/search.ejs')-%>
       <%-include('../views/partials/navbar.ejs')-%>
            <div class="container mt-5">
                <form class="FORM" action="/cal1/cal1/reservationConf" method="POST">
                    <div class="mb-3">
                        <label for="Date" class="label">Select Date:</label>
                        <input type="date" name="dateReservation" min="<%= new Date().toISOString().split('T')[0] %>"
                            class="InB form-control" />
                    </div>
                    <div class="mb-3">
                        <label for="Time" class="label">Select Time:</label>
                        <input type="time" name="timeReservation" class="InB form-control" />
                    </div>
                    <input type="hidden" name="productId" value="<%= productId %>" />
                    <h1><%= productId %></h1>
                    <button type="submit" class="BR btn btn-primary">Reservation</button>
                </form>
            </div>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</html>